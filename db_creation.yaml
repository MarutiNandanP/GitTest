# Create a new database with name 'jackdata'
- mssql_db:
    name: testansible
    state: present

# Copy database dump file to remote host and restore it to database 'my_db'
- copy:
    src: \\LAPTOP-0DB5TPTR\Anisble_DB\testansible.sql
    dest: /tmp

- mssql_db:
    name: my_db
    state: create
    target: /tmp/dump.sql
